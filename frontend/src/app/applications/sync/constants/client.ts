export enum CLIENT_SCHEDULER_STATE {
  DISABLED = 'disabled',
  ASYNC = 'async',
  SEQ = 'seq'
}

export enum CLIENT_APP_COUNTER {
  NOTIFICATIONS = 'notifications',
  TASKS = 'tasks',
  SYNCS = 'syncs'
}
