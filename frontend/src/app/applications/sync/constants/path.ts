export enum SYNC_PATH_ACTION {
  LIST = 'list',
  SYNC = 'sync',
  ADD = 'add',
  FLUSH = 'flush',
  SET = 'set',
  REMOVE = 'remove'
}

export enum SYNC_PATH_FILTER_TYPE {
  FILE = 'file',
  FOLDER = 'folder',
  EXPERT = 'expert',
  START = 'start',
  END = 'end',
  IN = 'in'
}
